{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "sum(rate(hprp_requests_total[1m]))", "legendFormat": "rps" }],
      "title": "Requests/sec",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "histogram_quantile(0.50, rate(hprp_request_duration_seconds_bucket[1m]))", "legendFormat": "p50" },
        { "expr": "histogram_quantile(0.95, rate(hprp_request_duration_seconds_bucket[1m]))", "legendFormat": "p95" },
        { "expr": "histogram_quantile(0.99, rate(hprp_request_duration_seconds_bucket[1m]))", "legendFormat": "p99" }
      ],
      "title": "Latency",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "targets": [{ "expr": "hprp_backend_healthy", "legendFormat": "{{backend}}" }],
      "title": "Backend Health",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["hprp"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "title": "HPRP Overview",
  "uid": "hprp-overview",
  "version": 1
}
